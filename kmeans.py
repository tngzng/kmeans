from typing import List

Point = List[int]


def kmeans(k: int, points: List[Point]) -> List[Point]:
    """
    return a list of cluster centroids, given a list of points and the
    number of clusters to identify
    """
    pass
